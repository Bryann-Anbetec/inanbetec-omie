{
  "name": "sync-cnpj-omie-to-inanbetec",
  "version": "1.0.0",
  "description": "Lambda para sincronização de clientes Omie → InAnbetec via CNPJ",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "zip -r function.zip . && aws lambda update-function-code --function-name sync-cnpj-omie-to-inanbetec --zip-file fileb://function.zip"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "typescript": "^5.0.0"
  }
}